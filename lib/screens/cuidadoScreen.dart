import 'package:flutter/material.dart';

class CuidadoScreen extends StatelessWidget {
  const CuidadoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cuidado Screen'),
      ),
      body: const Center(
        child: Text('Cuidado Screen'),
      ),
    );
  }
}
