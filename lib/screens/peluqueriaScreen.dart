import 'package:flutter/material.dart';

class PeluqueriaScreen extends StatelessWidget {
  const PeluqueriaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Peluqueria Screen'),
      ),
      body: const Center(
        child: Text('Peluqueria Screen'),
      ),
    );
  }
}
